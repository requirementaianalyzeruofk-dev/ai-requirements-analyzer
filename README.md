# Smart Home Access Requirements Analyzer 🏠🤖

هذا المشروع يقوم بتحليل وتصنيف متطلبات أنظمة البيوت الذكية (Smart Home)
إلى متطلبات **وظيفية (Functional)** و**غير وظيفية (Non-Functional)** باستخدام الذكاء الاصطناعي.

## 🧠 المكونات الأساسية:
- قاعدة بيانات `SQLite` لتخزين المتطلبات.
- واجهة رسومية مبنية بـ `Tkinter`.
- نموذج ذكاء اصطناعي مدرّب باستخدام `RoBERTa` لتصنيف المتطلبات.

## ⚙️ التثبيت:
بعد تنزيل المشروع، ثبّت المكتبات المطلوبة عبر:
```bash
pip install -r requirements.txt
